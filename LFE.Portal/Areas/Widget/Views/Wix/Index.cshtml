@model LFE.DataTokens.CoursePurchaseDTO

@{
    ViewBag.Title = Model.CourseName + " by " + Model.User.fullName;
    Layout = "~/Areas/widget/Views/Shared/_WidgetLayout.cshtml";
    
}
@{ var viewModel = (LFE.DataTokens.BaseModelViewToken)ViewBag.MainLayoutViewModel; }

@section css
{
    <link href="@Url.Action("CourseIntroCss", "Style", new { trackingID = viewModel != null && viewModel.WebStore != null ? viewModel.WebStore.TrackingID : "",
                                                             lastUpdate = viewModel != null && viewModel.WebStore != null ? (int)(viewModel.WebStore.LastUpdate - new DateTime(1970, 1, 1)).TotalSeconds : 0
           })" rel="stylesheet" />
    <script src="~/Areas/Widget/Scripts/lfe/lfe.global.js"></script>
  
}

@section JS
{
   @* <script type="text/javascript" src="http://admin.brightcove.com/js/BrightcoveExperiences.js"></script>
    <script type="text/javascript" src="http://admin.brightcove.com/js/APIModules_all.js"></script>*@
    @*<script>
        function loadIframe(iframeName, url) {             
            var $iframe = $('#' + iframeName);
            if ($iframe.length) {
                $iframe.attr('src', url);
                return false;
            }
            return true;
        }

        function ToggleBuyFrame() {
                var div = $('.dark');
                if ($('#buyFrameWrapper').is(":hidden")) {
                    div.fadeIn();
                  
                    $('#buyFrameWrapper').slideToggle();
                    var frameSrc = '@Url.Action("Buy", "Payment", new { Area = "Widget", courseName = Model.CourseUrlName })';

                    loadIframe('buyIframe', frameSrc);
                }
                else
                {
                    div.fadeOut();
                    $('#buyFrameWrapper').slideToggle();
                }

            }
    </script>*@
}


@Html.Partial("Course/_Course")